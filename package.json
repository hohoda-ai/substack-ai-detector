{
  "name": "substack-ai-detector",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Chrome extension that detects AI-generated content on Substack notes using a local transformer model.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "postinstall": "node scripts/copy-wasm.js"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.3.3"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.28",
    "@types/chrome": "^0.0.287",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
